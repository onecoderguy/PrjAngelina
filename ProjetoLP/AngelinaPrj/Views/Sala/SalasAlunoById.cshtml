@model IEnumerable<AngelinaPrj.Models.Aluno_Sala>

@{
    ViewBag.Title = "SalasAlunoById";
}

<h2>SalasAlunoById</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SalaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SalaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Aluno_SalaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Aluno_SalaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Aluno_SalaId })
        </td>
    </tr>
}

</table>
